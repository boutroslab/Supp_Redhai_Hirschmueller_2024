name: decode_env
channels:
  - conda-forge
  - bioconda
  # - defaults
dependencies:
  - python==3.11
  - snakemake
  - nextflow
  - scanpy
  - pandas
  - numpy
  - yaml
  - scrublet
  # pip install -U --extra-index-url https://db.erc-decode.eu/pypi decode-data-flow
  # Then configure the decode-data-flow package